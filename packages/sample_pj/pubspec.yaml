name: sample_pj
description: "donuts sample_pj."
version: 0.0.1

environment:
  sdk: ">=3.2.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  donuts_annotation:
    path: ../donuts_annotation
  donuts:
    path: ../donuts

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.6
  analyzer: ^5.13.0

flutter:
